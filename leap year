int main()
{
    int N;
    printf("Enter the year=");
    scanf("%d",&N);
    if((N%4==0&&N%100!=0)||N%400==0)
        printf("year is leap");
    else
        printf("year is not leap");
        return 0;
}
